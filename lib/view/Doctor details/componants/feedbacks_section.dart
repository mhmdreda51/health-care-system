import 'package:flutter/material.dart';

class FeedbacksSection extends StatelessWidget {
  const FeedbacksSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
